{{- if and (.Values.postgresql.enabled) (eq .Values.postgresql.mode "operator") }}
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: {{ .Values.postgresql.auth.database | default "justwms" }}-db
  namespace: postgres
spec:
  name: {{ .Values.postgresql.auth.database | default "justwms" }}
  owner: {{ .Values.postgresql.auth.username | default "justwms" }}
  cluster:
    name: {{ .Values.postgresql.operator.clusterName | default "master-postgres" }}
{{- end }}